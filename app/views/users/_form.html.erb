<%= form_for(@user) do |f| %>
  <% if @user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@user.errors.count, "error") %> prohibited this user from being saved:</h2>

      <ul>
      <% @user.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= "We need your mobile for this one ;)" %>
    <br/>
    <br/>
    <b><%= "Mobile" %><b/>
    <%= f.text_field :mobile, placeholder: '+639270000000' %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
